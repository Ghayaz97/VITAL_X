# Contributing to VITAL-X

## The Golden Rules

1. `main` is protected (demo-ready code only).
2. `dev` is our integration branch.
3. NEVER push directly to `main` or `dev`.

## Your Daily Workflow

1. **Get the latest code:**
   ```bash
   git checkout dev
   git pull origin dev
   ```
